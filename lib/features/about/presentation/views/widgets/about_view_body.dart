import 'package:flutter/material.dart'
    show BuildContext, Center, StatelessWidget, Text;

class AboutViewBody extends StatelessWidget {
  const AboutViewBody({super.key});
  @override
  Center build(BuildContext context) => const Center(child: Text('About View'));
}
