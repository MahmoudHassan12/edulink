import 'package:flutter/material.dart' show BorderRadius, Radius;

const double _common = 16;
const BorderRadius zeroBorder = BorderRadius.zero;
const xxsBorder = BorderRadius.all(Radius.circular(_common));
const xsBorder = BorderRadius.all(Radius.circular(_common + 4));
const sBorder = BorderRadius.all(Radius.circular(_common + 8));
const mBorder = BorderRadius.all(Radius.circular(_common + 12));
const lBorder = BorderRadius.all(Radius.circular(_common + 16));
const xlBorder = BorderRadius.all(Radius.circular(_common + 20));
const xxlBorder = BorderRadius.all(Radius.circular(_common + 24));
const circularBorder = BorderRadius.all(Radius.circular(100));
