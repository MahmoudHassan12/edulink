import 'package:flutter/material.dart' show BorderRadius, Radius;

const double _common = 16;
const BorderRadius zeroBorder = BorderRadius.zero;
const BorderRadius xxsBorder = BorderRadius.all(Radius.circular(_common));
const BorderRadius xsBorder = BorderRadius.all(Radius.circular(_common + 4));
const BorderRadius sBorder = BorderRadius.all(Radius.circular(_common + 8));
const BorderRadius mBorder = BorderRadius.all(Radius.circular(_common + 12));
const BorderRadius lBorder = BorderRadius.all(Radius.circular(_common + 16));
const BorderRadius xlBorder = BorderRadius.all(Radius.circular(_common + 20));
const BorderRadius xxlBorder = BorderRadius.all(Radius.circular(_common + 24));
const BorderRadius circularBorder = BorderRadius.all(Radius.circular(100));
